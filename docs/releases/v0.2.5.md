# remediation-core v0.2.5

## Changes

- Updates npm dependencies with `--package-lock-only` so remediation candidate scans are not polluted by generated `node_modules`.
- Keeps manual review diagnostic fields explicit in `result.json`.

## Compatibility

Use this release for production npm remediation runs where SBOM scans should be based on dependency manifests and lockfiles, not transient install trees.
